import styled from 'styled-components';

export const LimelightStyled = styled.div`
  background: rgba(0, 0, 0, 0.1);
  border-radius: 999px;
  
  padding: 80px;
  display: flex;
  justify-content: center;
  align-items: center;
`;
